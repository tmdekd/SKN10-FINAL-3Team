name: CI/CD fastapi_server_chatbot

on:
  push:
    paths:
      - 'fastapi_server/fastapi_server_chatbot/**'
      - '.github/workflows/ci-cd-server-chatbot.yaml'
    branches: [ main ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Chatbot server Build Docker image
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/fastapi_server_chatbot:latest
        context: ./fastapi_server/fastapi_server_chatbot
        file: ./fastapi_server/fastapi_server_chatbot/Dockerfile

    - name: Run Tests on Runpod Chatbot server
      uses: runpod/runpod-test-runner@v2.1.0
      with:
        image-tag: ${{ secrets.DOCKER_USERNAME }}/fastapi_server_chatbot:latest
        runpod-api-key: ${{ secrets.RUNPOD_API_KEY }}
        test-filename: fastapi_server/fastapi_server_chatbot/tests.json

    - name: Deploy to Runpod Chatbot server
      run: |
        curl 'https://rest.runpod.io/v1/pods/${{ secrets.CHATBOT_ROD_ID }}/reset' \
              --request POST \
              --header 'Authorization: Bearer ${{ secrets.RUNPOD_API_KEY }}'
